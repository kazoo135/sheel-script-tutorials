# This takes in the name of a file and changes it
# This can be done using positional arguments. They are like command line args
# Positional args are stored in system variables 
# $1 holds first arg after script, $2 holds second arg after script

mv $1 $2


