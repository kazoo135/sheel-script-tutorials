# gives the total number of items in the current working directory
# The script takes 1 command line arg '*' with set command
# This will set each item in current dir as a positional arg
# By using $# in script we return the total number of drs

echo The total number of items in current working dir are: $#
